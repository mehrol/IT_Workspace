name: Keep Website Awake

on:
  schedule:
    - cron: "*/28 * * * *"

  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Website
        run: |
          curl --fail --silent --show-error https://itworkspace.co.in